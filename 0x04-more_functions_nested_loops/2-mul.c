#include "main.h"

/**
 * mul - a function that multiplies two values
 * @a: first input
 * @b: second input
 * Return: multiplication rsult of the 2inputs
 */
int mul(int a, int b)
{
	return (a * b);
}
